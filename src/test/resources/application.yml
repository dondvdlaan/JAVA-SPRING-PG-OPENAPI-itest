spring:
  application:
    name: Decom-Admin
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    discovery:
      client:
        simple:
          instances:
            Decom-Admin:
              uri: http://localhost:7090
